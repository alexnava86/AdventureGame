using System;
using System.Collections;
using System.Collections.Generic;
using System.IO;
using System.Xml.Linq;
using Unity.Mathematics;
using UnityEngine;

public interface AbstractCharacterFactory //: MonoBehaviour
{
    //Player CreatePlayer();
    //Enemy CreateEnemy();
    //public NPC CreateNPC();

    /*
    public Player CreatePlayer()
    {
        Player character = ;
        //objects.Add((int)objectID + objectIDOffset, Resources.Load(path) as GameObject);
        //character = Instantiate(objects[(int)objectID], new Vector2(x, y), Quaternion.identity) as GameObject;
        return character;
    }
    */
    /*
    public NPC CreateNPC()
    {
        AbstractCharacter character;
        //objects.Add((int)objectID + objectIDOffset, Resources.Load(path) as GameObject);
        //character = Instantiate(objects[(int)objectID], new Vector2(x, y), Quaternion.identity) as GameObject;
    }
    */
    /*
    public Enemy CreateEnemy()
    {
        
    }
    */
}
